"""Every LIBERO family on the batched GPU env — model defs, budgets, envs.

    from noeira.envs.libero.models.libero_kitchen_scene3_xml import (
        LiberoKitchenScene3Model, LiberoKitchenScene3OscEnv,
        LIBERO_KITCHEN_SCENE3_OBS_DIM,
    )
    from noeira.envs.libero.models.budgets import LIBERO_GOAL_MAX_CONTACTS

`budgets.mojo` and the twenty `<family>_xml.mojo` are GENERATED by
`tools/libero/gen_libero_envs.mojo`; the `<family>_dims.mojo` beside them by
`tools/gen_model_dims.py`. `libero_goal`, `libero_object` and `libero_spatial`
keep their hand-written `envs/libero/models/libero_<suite>_xml.mojo`.
"""
