{
  "name": "mojo-rl-docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "pnpm run check:refs && astro build && pnpm run check:og",
    "preview": "astro preview",
    "check:refs": "node scripts/check-refs.mjs",
    "astro": "astro",
    "deploy": "pnpm run build && wrangler deploy",
    "preview:worker": "pnpm run build && wrangler dev",
    "check:og": "node scripts/check-og.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.5",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource-variable/manrope": "^5.3.0",
    "astro": "^7.0.2",
    "astro-og-canvas": "^0.13.0",
    "canvaskit-wasm": "^0.41.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "opentype.js": "^2.0.0",
    "wrangler": "^4.114.0"
  }
}
