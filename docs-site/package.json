{
  "name": "noeira-docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "pnpm run check:refs && astro build && cp root/* dist/ && pnpm run check:og",
    "preview": "astro preview",
    "check:refs": "node scripts/check-refs.mjs",
    "astro": "astro",
    "deploy": "pnpm run build && wrangler deploy",
    "deploy:redirect": "wrangler deploy --config redirect/wrangler.jsonc",
    "preview:worker": "pnpm run build && wrangler dev",
    "check:og": "node scripts/check-og.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.5",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource-variable/manrope": "^5.3.0",
    "astro": "^7.0.2",
    "astro-og-canvas": "^0.13.0",
    "canvaskit-wasm": "^0.41.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
