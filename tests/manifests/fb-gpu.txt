# FB GPU manifest — `pixi run -e nvidia test-fb-gpu` (or -e apple).
#
# The device path is gated against the CPU path, which carries the
# finite-difference checks in fb.txt. Tolerances were set FROM the measured
# parity (measure 7.1e-08, ortho 2.1e-07, actor 8.5e-08 on Apple), not
# defensively — see docs/BFM_ZERO_SHOT_RL.md §13 M2.
#
# ⚠ A GPU-vs-CPU band written on Metal is a TF32 trap on CUDA: compare weights
# bit-for-bit first if a 5090 run lands outside it.

# PairwiseDot / RowDot: M = F·B^T and its vjp (dA = G·B, dB = G^T·A) on device.
tests/nn/test_pairwise_dot.mojo

# One FBTrainer step, CPU vs GPU: losses, B(s), and smooth_action_t bit-exact.
tests/deep_agents/test_fb_trainer_gpu_parity.mojo

# One FBCPRTrainer step, CPU vs GPU (gp 0, sigma 0): the BCE halves, the
# style critic, the actor hook, D's logits and the expert encoding agree;
# then the full device sequence at gp 10 runs (measured 1e-7 on Apple).
tests/deep_agents/test_fb_cpr_gpu_parity.mojo

# The captured train step allocates nothing per call — any per-call alloc is a
# capture blocker, and the capture abort names only the FIRST one.
tests/deep_agents/test_fb_cuda_graph_safety.mojo

# The ONLINE agent (§18.3 A3) on a batched GPU Pendulum: lane z on the sphere
# and resampled, replay ring wraps, relabel_ratio keeps what it says, the
# driver runs end to end, two device steps advance the counters, and the
# checkpoint loads into the CPU FBTrainer the eval scripts build. ~5 min.
tests/deep_agents/test_fb_online_smoke.mojo

# The batched GPU walker raises `done` at MAX_STEPS on every lane — the env
# side of the `episodes 0` symptom of the first online run (§18.7). ~2 min.
tests/dm_control/test_walker_batched_truncation_gpu.mojo
