# Examples compile-probe manifest (`pixi run examples-compile`) — CI tier.
#
# Each entry is `mojo build -I .` ONLY (never executed — most would train).
# Keeps the root-level examples from silently rotting on toolchain/API
# migrations (13 of 23 had, before this manifest existed).
#
# Root examples with GPU device code are EXCLUDED for now: the CI `cpu` env
# has no GPU toolchain and their compile behavior there is unverified —
# probe them locally via `pixi run -e apple examples-compile-gpu` candidates
# if that tier ever gets added.

examples/bipedal_walker_demo.mojo
examples/car_racing_demo.mojo
examples/export_env_gifs.mojo
examples/gymnasium_demo.mojo
examples/hyperparam_search_demo.mojo
examples/mountain_car_demo_gif.mojo
examples/mountain_car_linear.mojo
examples/mountain_car_tiled.mojo
examples/pendulum_ddpg.mojo
examples/pendulum_sac.mojo
examples/pendulum_td3.mojo
examples/per_comparison_demo.mojo
examples/render_random_demo.mojo
examples/solve_cliffwalking.mojo
examples/solve_frozenlake.mojo
examples/solve_gridworld.mojo
examples/solve_mountaincar.mojo
examples/solve_pendulum.mojo
examples/solve_taxi.mojo

# ── the task-layer viewer ────────────────────────────────────────────
# ⚠ COMPILE-PROBED HERE, not run: it opens an SDL3 window. Its HEADLESS half
# is runnable and is the better smoke —
#   pixi run mojo run -I . examples/tasks/task_viewer.mojo <task> --check
# which loads the family, builds the model, resets from a sampled init,
# evaluates the goal and steps once, with no display.
examples/tasks/task_viewer.mojo

# ⚠ COMPILE-PROBED, NOT RUN — and not runnable on Apple at all. The family is
# nv=24, exactly where the P0 park probe died on Metal ("Compute function
# exceeds available stack space"): the physics kernels stack-allocate
# per-thread arrays sized by nv. It builds here and launches only on NVIDIA:
#   pixi run -e nvidia mojo run -I . examples/tasks/task_batched_gpu.mojo

# ⚠ NVIDIA ONLY, same nv=24 reason. Three runs: frozen table at two reset
# seeds (must agree, bit for bit) and a third that never applies the table
# (must differ). Run it with:
#   pixi run -e nvidia mojo run -I . examples/tasks/task_eval_frozen.mojo

# ⚠ Needs `references/LIBERO-master` (gitignored); SKIPS LOUDLY without it,
# and a skip is not a pass. Reports what LIBERO needs that we do not have.
examples/tasks/libero_survey.mojo
examples/tasks/task_eval_frozen.mojo
examples/tasks/task_batched_gpu.mojo

# GPU device code — local-only probes (see note above):
# examples/bipedal_walker_gpu_demo.mojo
# examples/bipedal_walker_cpu_gpu_compare.mojo
